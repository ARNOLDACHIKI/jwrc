# Database configuration
DATABASE_URL="postgresql://dev:devpass@localhost:5433/jwrc_db"
PGHOST=localhost
PGPORT=5433
PGUSER=dev
PGPASSWORD=devpass
PGDATABASE=jwrc_db

# Next.js env example (add more as needed)
# NEXT_PUBLIC_API_URL=http://localhost:3000/api

# JWT secret for signing auth tokens (change to a strong secret in production)
JWT_SECRET="replace_this_with_a_secure_random_value"
